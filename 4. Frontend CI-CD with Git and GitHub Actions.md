## Setting up Git and CI/CD for Frontend in AWS Cloud Resume Challenge

This document summarizes key themes and information from the provided transcript excerpt of "Setting up Git and CI/CD for Front end"

### Main Themes

1. **Troubleshooting:** A permission issue with the Lambda function from the previous part. The solution is to grant the Lambda function DynamoDB access through the AWS IAM console.

- "The issue that you might have ran into...is the permissions. So we didn't provide our Lambda function the permission to either get or update the records in dynamodb."

1. **Source Control with Git:** The importance of using a version control system like Git (or alternatives like GitLab, Bitbucket) is highlighted. The video demonstrates setting up a new GitHub repository, initializing a local repository, and pushing the website code.

- "The ideology is the same, you want to use some kind of source control or version control system."

1. **CI/CD with GitHub Actions:** The core focus is on setting up CI/CD for the frontend using GitHub Actions. A dedicated YAML file defines a workflow that automatically deploys the website to an AWS S3 bucket upon pushing changes to the main branch.

- "When we make changes to the website folder locally and we do git push the changes will be pushed to the repository but what I also want is for GitHub action to detect those changes and deploy those to AWS S3 right because that's where our front end is that's where our website is hosted so this part is known as CI CD and this is specifically for front end"

### Important Ideas/Facts

- **Lambda Function Permissions:** Lambda functions require explicit permissions to interact with other AWS services like DynamoDB.
- **Git Basics:** The video covers basic Git commands like git init, git add, git commit, and git push to manage the codebase.
- **GitHub Actions YAML:** CI/CD pipelines are defined in YAML files within a specific directory structure (.github/workflows) in the repository.
- **Secrets Management:** Sensitive information like AWS Access Key ID and Secret Access Key should be stored as secrets in GitHub to protect them.
- **GitHub Marketplace Actions:** The video leverages a pre-built GitHub Action (S3 Sync) from the marketplace to simplify the deployment process.

### Key Quotes

- "Make sure you add the readme with proper instructions and this covers the source control part of it now I want to move into setting up the CI CD where we will be using GitHub actions to deploy our website to S3 whenever we make changes"
- "GitHub actions you need to write yaml so we'll name this front and cicd Dot yaml and there we go so I'll time lapse this and then explain what the action is doing"
- "So in this video we covered setting up our source control which in the video was GitHub and then also how to use GitHub actions for CI CD for our front end in the next video we'll be doing the IEC stuff and CI CD for our backend"

### Next Steps

- Implement the Git repository setup and the initial commit of the frontend code.
- Create the GitHub Actions workflow YAML file based on the example provided in the video, ensuring necessary customization for the specific project.
- Configure GitHub secrets for the AWS credentials and S3 bucket name.
- Test the CI/CD pipeline by pushing changes to the repository and verifying the automatic deployment to the S3 bucket.

